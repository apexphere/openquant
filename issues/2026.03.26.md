核心架构：
Strategy 基类（1752 行）— 所有策略继承它，定义 should_long/short、go_long/short、update_position 等生命周期方法
Modes — backtest / optimize（Optuna）/ monte carlo / import candles
FastAPI 后端 — 给 dashboard 前端用，端口 9000，带 WebSocket、LSP、认证
Exchange 支持 — Bybit、Binance、Coinbase、Gate、Bitget、dYdX、Hyperliquid 等
300+ 技术指标 — indicators/ 目录
ML 模块 — 已经内建了 gather → train → deploy 的流程，支持 binary/multiclass/regression，用 sklearn + 时间序列交叉验证

